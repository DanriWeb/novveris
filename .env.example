# Root Environment Variables
# This file contains shared environment variables for the monorepo

# Database Configuration (shared)
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/novveris

# Note: Each app (frontend/backend) has its own .env.example file
# Copy this file to .env and fill in your actual values
# Then copy apps/frontend/.env.example to apps/frontend/.env
# And copy apps/backend/.env.example to apps/backend/.env
